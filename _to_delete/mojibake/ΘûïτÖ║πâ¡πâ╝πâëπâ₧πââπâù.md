# 開発ロードマップ

要件定義書 v3.0 第2章「段階導入計画」に対応する実作業のタスク表。
**各フェーズの完了条件を満たすまで次に進まない。**

進捗確認: `python -m flightdeal.main --status`
手動テストの手順: [`テスト手順.md`](テスト手順.md)

---

## Phase 0 — 技術検証

> 完了条件: いずれかの方式で3日連続、価格・時刻・航空会社名が取得できる

| # | タスク | 成果物 | 状態 |
|---|---|---|---|
| 0-1 | fast-flights の最新リリース日・Issue の状況を確認 | `docs/phase0_log.md` に記録 | ⬜ |
| 0-2 | `pip install -r requirements.txt` が通ることを確認 | — | ⬜ |
| 0-3 | `scripts/phase0_check.py --dest HIJ` を実行し取得可否を確認 | 実行ログ（`テスト手順.md` T8） | ⬜ |
| 0-4 | 取得項目（航空会社名 / 出発時刻 / 到着時刻 / 価格）が揃うか確認 | チェックリスト記入 | ⬜ |
| 0-5 | 直行便への絞り込み可否を確認（不可ならローカル除外に切替） | 方針をメモ | ⬜ |
| 0-6 | 3日間、同じ検証を繰り返して安定性を確認 | `phase0_log.md` に3日分 | ⬜ |
| 0-7 | 生レスポンスを `--save` で `tests/fixtures/raw/` に保存 | フィクスチャ | ⬜ |
| 0-8 | **判断**: fast-flights 採用 or Playwright へ切替 | 決定を記録 | ⬜ |

**0-8 で不採用となった場合**: `src/flightdeal/fetchers/playwright_fetcher.py` を新規作成し、
`fetchers/__init__.py` の `build_fetcher` に登録する（インターフェースは変更不要）。
`requirements.txt` の Playwright 行のコメントアウトを解除する。

---

## Phase 1 — MVP（2路線・第1週末・ドライラン）

> 完了条件: 判定ロジックの単体テスト全通過 ＋ 1週間の手動実行で妥当な判定結果

**1-1 〜 1-16 は実装・テスト済み**（テスト108件が全通過、`--dry-run` が端から端まで通る）。
残るのは 1-17（実運用での妥当性確認）のみ。

| # | タスク | 対象ファイル | テスト | 状態 |
|---|---|---|---|---|
| 1-1 | 週末日付の算出 | `weekend.py` | `test_weekend.py` | ✅ |
| 1-2 | 価格の正規化 | `pricing.parse_price` | `test_pricing.py` | ✅ |
| 1-3 | 航空会社の区分判定と補正額 | `pricing.classify_airline` / `surcharge_for` | 同上 | ✅ |
| 1-4 | 実質価格の算出 | `pricing.effective_price` | 同上 | ✅ |
| 1-5 | 時間帯フィルター | `filters.filter_outbound` / `filter_return` | `test_filters.py` | ✅ |
| 1-6 | 組み合わせ生成 | `filters.combine` | 同上 | ✅ |
| 1-7 | ランク判定（境界値に注意） | `ranking.judge_rank` | `test_ranking.py` | ✅ |
| 1-8 | 上位N件の抽出 | `ranking.top_n_per_route` | 同上 | ✅ |
| 1-9 | fast-flights 取得実装 | `fetchers/fast_flights_fetcher.py` | パース部のみ ✅ / 実接続は Phase 0 待ち | ⚠️ |
| 1-10 | リトライとランダム待機 | `pipeline.fetch_with_retry` | `test_pipeline.py` | ✅ |
| 1-11 | 路線単位の処理と隔離 | `pipeline.process_route` | 同上 | ✅ |
| 1-12 | 全体パイプライン | `pipeline.run` | 同上 | ✅ |
| 1-13 | 検索URL生成・日付表記 | `formatter.search_url` / `format_date_range` | `test_formatter.py` | ✅ |
| 1-14 | 通知本文の組み立て | `formatter.format_combination` / `format_run` | 同上 | ✅ |
| 1-15 | `--dry-run` が最後まで通ることを確認 | — | 目視 | ✅ |
| 1-16 | 全テスト通過（108件） | `tests/` | `pytest` | ✅ |
| 1-17 | 1週間、毎日手動実行して判定の妥当性を目視確認 | — | `テスト手順.md` T8 | ⬜ |

### 1-17 の見るべきポイント
- 相場（`market_price`）の初期値は妥当か。高すぎるとSランクが乱発され、低すぎると何も通知されない
- 時間帯フィルター後に便が0件になる路線がないか
- LCC補正3,500円が実勢と乖離していないか

---

## Phase 2 — 通知・自動化

> 完了条件: 3日連続で自動実行が成功（または失敗通知が正しく届く）

| # | タスク | 対象 | 状態 |
|---|---|---|---|
| 2-1 | LINE Developers で Messaging API チャネルを作成 | — | ⬜ |
| 2-2 | チャネルアクセストークン（長期）を発行 | — | ⬜ |
| 2-3 | 自分の userId を Webhook イベントから取得 | — | ⬜ |
| 2-4 | `.env` を作成し `--test-notify` で疎通確認 | `テスト手順.md` T9 | ⬜ |
| 2-5 | Push 送信の実装 | `notifiers/line.py` | ✅（未疎通） |
| 2-6 | 5,000文字分割の実装 | `formatter.split_message` | ✅ |
| 2-7 | 全路線失敗時の障害通知 | `formatter.format_failure` | ✅ |
| 2-8 | GitHub リポジトリを private で作成し push | — | ⬜ |
| 2-9 | Secrets に `LINE_CHANNEL_ACCESS_TOKEN` / `LINE_TO_USER_ID` を登録 | — | ⬜ |
| 2-10 | `config.json` の `fetcher` を `fast_flights`、`notifier` を `line` に変更 | — | ⬜ |
| 2-11 | `workflow_dispatch` で手動実行して成功を確認 | — | ⬜ |
| 2-12 | 3日連続で日次実行が成功することを確認 | — | ⬜ |
| 2-13 | LINE無料枠の現行上限を確認（要件R6） | — | ⬜ |

### 運用切替の判断（要件4.1）
**全路線の取得失敗が3日連続**したら、Bot判定によるIPブロックとみなしローカル実行へ切り替える。
手順は [`運用手順.md`](運用手順.md) を参照。

---

## Phase 3 — 拡張（全5路線・第2週末）

> 完了条件: 1週間の安定稼働

| # | タスク | 状態 |
|---|---|---|
| 3-1 | 福岡・新千歳・那覇の `market_price` / `max_price` を実勢に合わせて調整 | ⬜ |
| 3-2 | `config.json` の `enabled` を全 true に変更 | ⬜（ドライランで動作確認済み） |
| 3-3 | `weekends_ahead` を 2 に変更 | ⬜（ドライランで動作確認済み） |
| 3-4 | 検索20回でも `timeout-minutes: 30` に収まるか確認 | ⬜ |
| 3-5 | 待機時間（5〜15秒）を実測に合わせて調整 | ⬜ |
| 3-6 | 1週間の安定稼働を確認 | ⬜ |

> Phase 3 はコード変更なしで移行できる設計。`config.json` の変更だけで完結すること自体が
> 設計の検証になる。もしコード修正が必要になったら、それは設計の不備なので記録して直す。

---

## 将来拡張（スコープ外・要件7）

- 祝日・3連休対応（金曜夜発・月曜帰着）
- 価格履歴の蓄積と相場の自動算出 — `market_price` の手動調整が不要になる
- 重複通知の抑制（前回通知との差分検知）
- 公式API（Amadeus Self-Service等）への移行 ※国内LCCのカバレッジが課題
- 出発地の複数化
