# Weekend cheap-flight finder / LINE notifier
# Python 3.11+ (fast-flights itself requires >=3.10).

# --- required ---
requests==2.32.3          # LINE Messaging API Push / generic HTTP

# --- data source (priority 1: req 3.2.2) ---
# PINNED. This project uses the 3.x API (FlightQuery / create_filter / parser.parse).
# Version 2.x has a completely different API (FlightData) and will NOT work.
# Verified working: 3.0.2 (Phase 0, 2026-07).
fast-flights==3.0.2

# --- data source (priority 2: fallback, not adopted / commented out) ---
# playwright==1.48.0
# playwright-stealth==1.0.6

# --- dev / test ---
pytest==8.3.3
