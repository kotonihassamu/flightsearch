# 週末格安航空券 自動発見・LINE通知システム 要件定義書（v3.0 実現可能性精査版）

| 項目 | 内容 |
|---|---|
| 文書バージョン | v3.0 |
| 作成日 | 2026年7月26日 |
| 位置づけ | v2.0を第三者観点で精査し、実現可能性の低い要件を修正・再構成した版 |

---

## 0. 精査結果サマリー（v2.0の主要な問題点と修正方針）

第三者レビューにより、v2.0には「作れるが、安定運用できない」要件が複数含まれていると判断した。

| # | v2.0の問題点 | 実現可能性への影響 | v3.0での修正 |
|---|---|---|---|
| P1 | **往復検索の組み合わせ抽出をスクレイピングで行う設計**。Google Flightsの往復検索は「往路を選択→復路一覧が表示」という2段階UIのため、全組み合わせの取得は操作ステップが多く、最も壊れやすい | 致命的（開発難度・保守コストが跳ね上がる） | **片道検索×2回（土曜HND→現地、日曜現地→HND）に分解**し、組み合わせと合算はローカルで計算する。日本の国内線は片道運賃の単純合算が基本のため精度への影響は軽微 |
| P2 | **自然言語クエリ（?q=Flights from...）への依存**。リダイレクトや検索解釈のブレがあり、日付・空港が意図通り反映されない場合がある | 高（取得の不確実性） | データ取得を抽象化レイヤーの背後に置き、第一候補として**Google Flightsの内部リクエストを利用する既存ライブラリ（例: fast-flights）**、第二候補としてPlaywrightスクレイピングを位置づける。採用可否は開発着手時に動作検証して決定 |
| P3 | **GitHub ActionsからのGoogleアクセスはBot判定されやすい**（データセンターIP）。stealth対策でも回避を保証できない | 高（日次実行の成立性） | GitHub Actionsを第一候補としつつ、**「連続3日失敗したらローカル実行（自宅PC/Raspberry Piのcron）へ切替」という運用判断基準を要件に含める**。切替可能なよう環境非依存の実装とする |
| P4 | 全5路線×2週末×往復＝処理量が多く、Bot判定リスクとジョブ時間を増大させる | 中 | **MVPは2路線に限定**し、段階導入（フェーズ分け）を要件化する |
| P5 | Google Flightsの「予約直リンク」「検索結果URL」は将来のURL仕様変更に弱い | 低 | 通知リンクは「日付・空港を埋めたGoogle Flightsトップの検索URL」とし、リンク切れしても通知本文だけで行動できる情報量を担保する |
| P6 | 受け入れ基準がE2E偏重で、外部サイト依存部分のテストが不安定 | 中 | パース部を**保存済みHTML/レスポンスによるフィクスチャテスト**で検証する方針に変更 |

---

## 1. システム概要・目的

羽田空港（HND）発の週末「弾丸旅行」（土曜朝発・日曜夜帰着）向け格安国内航空券を自動検索・評価し、お得な便だけをLINEに通知するシステム。

1. **片道運賃×2の合算**による往復実質価格の算出（LCC手荷物補正込み）
2. 路線ごとの「通常相場」との比較による安さランク判定（S/A/B）
3. 該当便がある日のみのLINE通知（1日1回実行）

### 1.1. 想定ユーザーと前提
- 個人利用（1名）。マルチユーザー対応はスコープ外
- 表示価格と実際の予約価格の差異、および非公式なデータ取得手段に伴う取得失敗は**許容する**（本システムは「発見の補助」であり、最終確認は利用者が予約サイトで行う）

---

## 2. 段階導入計画（フェーズ定義）

実現可能性を確保するため、以下の順に段階的に構築する。**各フェーズの完了条件を満たすまで次に進まない。**

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| Phase 0（技術検証） | データ取得方式の検証。fast-flights等ライブラリとPlaywrightの両方で、HND→広島の片道価格が取得できるか手元PCで確認 | いずれかの方式で3日連続、価格・時刻・航空会社名が取得できる |
| Phase 1（MVP） | 2路線（広島・松山）・第1週末のみ・通知はコンソール出力（ドライラン） | 判定ロジックの単体テスト全通過＋1週間の手動実行で妥当な判定結果 |
| Phase 2（通知・自動化） | LINE通知接続、GitHub Actionsでの日次実行 | 3日連続で自動実行が成功（または失敗通知が正しく届く） |
| Phase 3（拡張） | 全5路線・第2週末を追加。処理間の待機・負荷調整 | 1週間の安定稼働 |

---

## 3. 機能要件

### 3.1. 検索条件

- **出発地**: 羽田空港（HND）固定
- **目的地**: 設定ファイルで管理（Phase 1: 広島 HIJ・松山 MYJ / Phase 3で福岡 FUK・新千歳 CTS・那覇 OKA を追加）
- **対象日程**: 実行日を基準に、次に到来する土曜日を「第1週末」、その翌週を「第2週末」とする（実行日が土曜の場合は当日を含めず翌週土曜を第1週末とする）。Phase 1は第1週末のみ
- **便種別**: 直行便のみ
- **祝日・3連休**: 考慮しない（将来拡張候補）

### 3.2. データ取得方式（v3.0で再設計）

#### 3.2.1. 片道分解方式（本要件の中核）
往復検索は行わず、**片道検索を2回**実行する。

1. 往路検索: HND → 目的地、対象土曜日
2. 復路検索: 目的地 → HND、対象日曜日

取得した片道便リストから、時間帯フィルターを満たす便同士を**ローカルで総当たり組み合わせ**、片道価格の合算＋手荷物補正で「実質価格（往復）」を算出する。

> 根拠: 日本の国内線は往復割引が原則廃止されており片道運賃の合算と実売価格の乖離が小さいこと、および片道検索は1ページで完結しスクレイピング難度が大幅に下がることによる。

#### 3.2.2. データソースの抽象化
データ取得部は `FlightFetcher` インターフェース（入力: 出発地・到着地・日付 / 出力: 便リスト）として実装を差し替え可能にし、以下の優先順で採用する。

| 優先 | 方式 | 特徴 | 採用判断 |
|---|---|---|---|
| 1 | Google Flights系の既存取得ライブラリ（例: fast-flights） | ブラウザ不要・軽量・高速。ただし非公式でメンテ状況に依存 | Phase 0で最新のメンテ状況・動作を確認して判断 |
| 2 | Playwright（headless Chromium）+ stealth | 柔軟だが重く、Bot判定・構造変化に弱い | 方式1が不採用/故障時のフォールバック |

- **取得項目**: 航空会社名、出発/到着時刻、片道価格（JPY）、便名（取得可能な場合）
- 価格はカンマ・通貨記号を除去し整数化。取得できない項目がある便は判定対象から除外し、除外件数をログに記録する

### 3.3. フライト時間帯フィルター

- **往路（土曜）**: 現地到着時刻が 11:00 以前
- **復路（日曜）**: 現地出発時刻が 17:00 以降
- 時刻はJST。閾値は設定ファイルで変更可能

### 3.4. LCC手荷物料金の自動補正

片道分解方式に合わせ、**補正は片道単位**で行う。

| 区分 | 対象（初期値） | 補正額（片道） |
|---|---|---|
| FSC・中堅 | ANA, JAL, Skymark, Solaseed Air, AIRDO, StarFlyer | +0円 |
| LCC | Peach, Jetstar, Spring Japan | +3,500円 |
| 判定不能 | リスト外の航空会社 | LCC扱い（+3,500円）とし通知に「補正額は仮」と注記 |

- 実質価格（往復）＝ 往路価格＋往路補正＋復路価格＋復路補正
- マッピング・補正額は設定ファイルで管理

### 3.5. 安さランク判定ロジック

上から順に判定し、最初に該当したランクを採用する。

| ランク | 条件 |
|---|---|
| S | 実質価格 ≤ 通常相場 × 0.70 |
| A | 実質価格 ≤ 通常相場 × 0.85 |
| B | 実質価格 < 通常相場 かつ 実質価格 ≤ max_price |
| ランク外 | 通知しない |

- 同一路線・同一週末でランク入りが複数ある場合、実質価格の安い順に**上位3件**まで通知
- 閾値は設定ファイルで変更可能

### 3.6. 通知機能

- **ツール**: LINE Messaging API（Push Message）※LINE Notifyはサービス終了済みのため不可
- **トリガー**: Bランク以上が1件以上ある場合のみ。0件の日は通知しない
- **重複制御**: 行わない（同じ便が毎日通知されうることを仕様として許容）
- **集約**: 全結果を1通に集約。5,000文字超の場合のみ分割
- **通知内容**: 路線・日付・ランク、実質価格・相場・割引率、往復の時刻と航空会社、補正注記、検索URL

#### 通知フォーマット例

```
【S】羽田⇔広島 8/1(土)〜8/2(日)
実質 ¥22,800（相場 ¥35,000 / ▲35%）
往路: ANA 07:25発 → 08:50着（¥11,400）
復路: ANA 19:05発 → 20:30着（¥11,400）
補正: なし（FSC）
検索: https://www.google.com/travel/flights?hl=ja&curr=JPY&q=HND to HIJ 2026-08-01
```

- リンクはあくまで補助とし、**リンクが機能しなくても本文情報だけで航空会社サイト等から予約行動に移れる**情報量を必須とする

---

## 4. 非機能要件

### 4.1. 実行環境と運用切替基準

- **第一候補**: GitHub Actions（cron: JST 8:00 = `0 23 * * *` UTC、遅延許容、`timeout-minutes: 30`、`workflow_dispatch`による手動実行可）
- **切替基準（要件として明記）**: データ取得の**全路線失敗が3日連続**した場合、Bot判定によるIPブロックとみなし、ローカル環境（自宅PC / Raspberry Pi のcron）での実行に切り替える
- 上記切替を可能にするため、実装は環境非依存（環境変数で秘匿情報を注入、OS依存処理なし）とする

### 4.2. 技術スタック

- Python 3.11以降、依存は `requirements.txt` でバージョン固定
- データ取得: 3.2.2の抽象化レイヤーに従う（Playwright採用時のみブラウザ依存を追加）

### 4.3. 安定性・エラーハンドリング

- **路線単位の隔離**: 1路線（1検索）の失敗が全体を停止させない（try-except＋継続）
- **リトライ**: 検索単位で最大2回、待機を挟んで再試行
- **負荷抑制**: 検索間にランダム待機（5〜15秒）。1実行あたりの検索回数は最大 路線数×2週末×2方向＝20回に制限
- **失敗の可視化**: 全路線失敗時は「取得失敗」をLINE通知（サイレント障害防止）。Playwright採用時は失敗路線のスクリーンショット/HTMLをActionsアーティファクトに保存（保持7日）
- **ログ**: 構造化ログ（路線、取得件数、除外件数、判定結果、所要時間）を標準出力へ

### 4.4. セキュリティ

- LINEチャネルアクセストークン・宛先ユーザーIDは GitHub Secrets（ローカル実行時は環境変数/.env）で管理し、コード・ログに出さない

### 4.5. 保守性（設定の分離）

`config.json` で以下を一元管理する。

```json
{
  "origin": "HND",
  "destinations": [
    {"iata": "HIJ", "name": "広島", "market_price": 35000, "max_price": 30000, "enabled": true},
    {"iata": "MYJ", "name": "松山", "market_price": 33000, "max_price": 28000, "enabled": true},
    {"iata": "FUK", "name": "福岡", "market_price": 30000, "max_price": 26000, "enabled": false},
    {"iata": "CTS", "name": "新千歳", "market_price": 32000, "max_price": 28000, "enabled": false},
    {"iata": "OKA", "name": "那覇", "market_price": 40000, "max_price": 34000, "enabled": false}
  ],
  "weekends_ahead": 1,
  "time_filter": {"outbound_arrive_by": "11:00", "return_depart_after": "17:00"},
  "baggage_surcharge_oneway": 3500,
  "lcc_airlines": ["Peach", "Jetstar", "Spring Japan"],
  "fsc_airlines": ["ANA", "JAL", "Skymark", "Solaseed", "AIRDO", "StarFlyer"],
  "rank_thresholds": {"S": 0.70, "A": 0.85},
  "notify_top_n_per_route": 3
}
```

- `enabled` フラグと `weekends_ahead` によりフェーズ移行をコード変更なしで行えること

---

## 5. 制約事項・リスク（残存リスク）

| # | リスク | 対応 |
|---|---|---|
| R1 | Google Flightsのデータ取得は非公式手段であり、規約抵触の可能性と恒久的な動作保証がない | 個人利用・1日1回・最大20検索に抑制。取得部を抽象化し、将来の公式API（Amadeus等）や他ソースへの移行余地を確保 |
| R2 | GitHub ActionsのIPがブロックされる可能性 | 4.1の切替基準に従いローカル実行へ移行 |
| R3 | 片道合算価格と実際の往復予約価格に差が出るケース（キャンペーン運賃等） | 「発見の補助」という位置づけを明示。最終価格は利用者が予約時に確認 |
| R4 | LCC補正の一律加算による誤差 | 設定で調整可能＋通知に注記を必須化 |
| R5 | 取得ライブラリ（fast-flights等）のメンテ停止 | Phase 0で最新状況を確認して採否判断。フォールバックとしてPlaywright実装を維持 |
| R6 | LINE Messaging API無料枠の月間送信数上限 | 1日最大1〜2通に集約。導入時に現行の無料枠を確認 |

---

## 6. テスト・受け入れ基準

1. **単体テスト（pytest）**: 週末日付算出・時間帯フィルター・組み合わせ生成・補正計算・ランク判定を純粋関数として実装し、以下を検証する
   - 実行曜日が金/土/日の場合の週末算出
   - 相場の70% / 85% / max_price ちょうどの境界値
   - 往路LCC＋復路FSCなど混在時の補正
2. **フィクスチャテスト**: 実際に保存した取得結果（HTML/レスポンス）を入力としたパーステスト。外部サイトへの接続を伴うテストはCIの合否条件に含めない
3. **フェーズ完了条件**: 2章の各フェーズ完了条件をもって受け入れとする

---

## 7. スコープ外・将来拡張候補

- 祝日・3連休対応（金曜夜発・月曜帰着）
- 価格履歴の蓄積と相場の自動算出
- 重複通知の抑制（前回通知との差分検知）
- 公式API（Amadeus Self-Service等）への移行 ※国内LCCのカバレッジが課題のため要調査
- 出発地・検索条件の複数化
